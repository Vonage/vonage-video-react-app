/**
 * List of Federated Assertions and prefixes used in JWTs signed by Portunus
 */
const VIDEO_IDENTIFIER = 'video-api';
const IDENTIFIERS = [VIDEO_IDENTIFIER];
const ISSUER_PREFIX = 'VIAM';

module.exports = {
  VIDEO_IDENTIFIER,
  IDENTIFIERS,
  ISSUER_PREFIX,
};
