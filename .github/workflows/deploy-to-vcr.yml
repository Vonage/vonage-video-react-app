name: Deploy to VCR

on:
  pull_request:
    branches:
      - develop
    types: [closed]

permissions:
  contents: read

env:
  DOMAIN: ${{secrets.DOMAIN}}

jobs:
  build-and-test:
    if: github.event.pull_request.merged == true
    runs-on: [vcp-runner]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Verify Node & Yarn versions
        run: |
          node -v
          yarn -v

      - name: Building artefact
        run: |
          sed -i "s/<DOMAIN>/${DOMAIN}/g" "${{ github.workspace }}/vcr-gha.yml"
          ./vcrBuild.sh

      - name: Install VCR CLI
        shell: bash
        run: |
          set -euo pipefail
          curl --proto =https --max-redirs 0 -fsSL https://raw.githubusercontent.com/Vonage/cloud-runtime-cli/main/script/install.sh -o install.sh
          sudo bash install.sh
          vcr -v

      - name: Deploy vcr
        env:
          APP_ID: ${{ secrets.APP_ID }}
          VCR_API_KEY: ${{ secrets.VCR_API_KEY }}
          VCR_API_SECRET: ${{ secrets.VCR_API_SECRET }}
        run: |
          cd backend/dist

          vcr deploy \
            --filename vcr-gha.yml \
            --app-id "$APP_ID" \
            --api-key "$VCR_API_KEY" \
            --api-secret "$VCR_API_SECRET" \
            --region aws.euw1 \
            --graphql-endpoint https://graphql.euw1.runtime.vonage.cloud/v1/graphql \
            --timeout=15m \
            2>&1 | tee deploy-vcr-logs.log

          echo "Checking if the deploy job is successful"
          if grep -Fxq '| Instance has been deployed!' ./deploy-vcr-logs.log ; then
            echo "success in deployment"
            exit 0
          else
            echo "failure in deployment"
            exit 1
          fi

      - name: Prepare Slack payload variables
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_NUMBER: ${{ github.event.number }}
          COMMIT_SHA: ${{ github.sha }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
        run: |
          echo "PR_TITLE=$PR_TITLE" >> $GITHUB_ENV
          echo "PR_NUMBER=$PR_NUMBER" >> $GITHUB_ENV
          echo "COMMIT_SHA=$COMMIT_SHA" >> $GITHUB_ENV
          echo "REPO=$REPO" >> $GITHUB_ENV
          echo "RUN_ID=$RUN_ID" >> $GITHUB_ENV

      - name: Slack notification on success
        uses: slackapi/slack-github-action@70cd7be8e40a46e8b0eced40b0de447bdb42f68e # v1.26.0
        with:
          channel-id: '#video-solutions-ci-notifications'
          payload: |
            {
              "text": "Vera Deployment GHA Job",
              "attachments": [
                {
                  "color": "28a745",
                  "text": "Vera pull request *<https://github.com/Vonage/vonage-video-react-app/pull/${PR_NUMBER}|${PR_TITLE}>*\nwith commit hash: ${COMMIT_SHA}\nis merged and deployed to VCR\n*<https://github.com/${REPO}/actions/runs/${RUN_ID}|Click here to go to build logs>*\n:large_green_circle: :large_green_circle: :large_green_circle: :large_green_circle:"
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATION }}
        if: success()
        continue-on-error: true

      - name: Slack notification on failure
        uses: slackapi/slack-github-action@70cd7be8e40a46e8b0eced40b0de447bdb42f68e # v1.26.0
        with:
          channel-id: '#video-solutions-ci-notifications'
          payload: |
            {
              "text": "Vera Deployment GHA Job",
              "attachments": [
                {
                  "color": "FF0000",
                  "text": "Vera pull request *<https://github.com/Vonage/vonage-video-react-app/pull/${PR_NUMBER}|${PR_TITLE}>*\nwith commit hash: ${COMMIT_SHA}\nis merged but FAILED to deploy to VCR\n*<https://github.com/${REPO}/actions/runs/${RUN_ID}|Click here to go to build logs>*\n:red_circle: :red_circle: :red_circle: :red_circle:"
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATION }}
        if: failure()
        continue-on-error: true
