#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# vonage-git-ai: post-commit AI attribution
if command -v vonage-git-ai &>/dev/null; then
  _VGAI_LIB="$(dirname "$(command -v vonage-git-ai)")/../lib/copilot-checkpoint.sh"
  if [[ -f "$_VGAI_LIB" ]]; then
    source "$_VGAI_LIB"
    vgai_copilot_post_commit || true
  fi
fi

