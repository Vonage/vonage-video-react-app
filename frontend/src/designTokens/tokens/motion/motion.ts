/**
 * Motion Design Tokens
 *
 * This module aggregates motion-related design tokens,
 * specifically duration and easing, to standardize animation behaviors
 * across the user interface.
 */
import duration from './duration';
import easing from './easing';

const motion = {
  duration,
  easing,
};

export default motion;
